# GateLM Application server env
#
# Copy this file to `.env.local` in the same directory for local application use.
# Do not commit `.env.local`.
# Do not use NEXT_PUBLIC_ for Gateway credentials.

GATELM_GATEWAY_BASE_URL=http://localhost:8080
GATELM_GATEWAY_API_KEY=replace_with_project_api_key
GATELM_GATEWAY_APP_TOKEN=replace_with_application_app_token

# Keep this as auto so the Gateway Runtime Policy / Routing selects the model.
GATELM_APPLICATION_CHAT_MODEL=auto
