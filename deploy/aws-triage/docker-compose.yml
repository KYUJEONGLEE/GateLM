name: gatelm-aws-triage

services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$$POSTGRES_USER\" -d \"$$POSTGRES_DB\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    networks:
      - gatelm_internal

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - gatelm_internal

  mock-provider:
    image: python:3.12-alpine
    restart: unless-stopped
    environment:
      MOCK_PROVIDER_PORT: "8090"
      MOCK_PROVIDER_DEFAULT_LATENCY_MS: ${MOCK_PROVIDER_DEFAULT_LATENCY_MS}
    command:
      - python
      - -c
      - |
        import json, os, time
        from http.server import BaseHTTPRequestHandler, HTTPServer

        port = int(os.getenv("MOCK_PROVIDER_PORT", "8090"))
        latency_ms = int(os.getenv("MOCK_PROVIDER_DEFAULT_LATENCY_MS", "100"))

        class Handler(BaseHTTPRequestHandler):
            def log_message(self, format, *args):
                return

            def send_json(self, status, payload):
                body = json.dumps(payload).encode("utf-8")
                self.send_response(status)
                self.send_header("Content-Type", "application/json")
                self.send_header("Content-Length", str(len(body)))
                self.end_headers()
                self.wfile.write(body)

            def do_GET(self):
                if self.path == "/healthz":
                    self.send_json(200, {"status": "ok"})
                    return
                if self.path == "/v1/models":
                    self.send_json(200, {"object": "list", "data": [
                        {"id": "mock-fast", "object": "model", "owned_by": "gatelm-aws-triage"},
                        {"id": "mock-balanced", "object": "model", "owned_by": "gatelm-aws-triage"},
                        {"id": "mock-smart", "object": "model", "owned_by": "gatelm-aws-triage"}
                    ]})
                    return
                self.send_json(404, {"error": {"code": "not_found"}})

            def do_POST(self):
                if self.path != "/v1/chat/completions":
                    self.send_json(404, {"error": {"code": "not_found"}})
                    return
                length = int(self.headers.get("Content-Length", "0"))
                raw = self.rfile.read(length) if length else b"{}"
                try:
                    payload = json.loads(raw.decode("utf-8"))
                except Exception:
                    payload = {}
                time.sleep(latency_ms / 1000)
                model = str(payload.get("model") or "mock-balanced").strip() or "mock-balanced"
                self.send_json(200, {
                    "id": "mock_chatcmpl_aws_triage",
                    "object": "chat.completion",
                    "created": int(time.time()),
                    "model": model,
                    "choices": [{
                        "index": 0,
                        "message": {
                            "role": "assistant",
                            "content": "Mock response generated by GateLM AWS triage mock provider."
                        },
                        "finish_reason": "stop"
                    }],
                    "usage": {"prompt_tokens": 1, "completion_tokens": 1, "total_tokens": 2}
                })

        HTTPServer(("0.0.0.0", port), Handler).serve_forever()
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8090/healthz', timeout=2).read()\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - gatelm_internal

  control-plane-api:
    image: gatelm/control-plane-api:aws-triage
    build:
      context: ../..
      dockerfile: infra/docker/control-plane-api.Dockerfile
    restart: unless-stopped
    environment:
      CONTROL_PLANE_PORT: "3001"
      CONTROL_PLANE_ADMIN_AUTH_MODE: ${CONTROL_PLANE_ADMIN_AUTH_MODE}
      CONTROL_PLANE_WEB_ORIGIN: ${GATELM_PUBLIC_BASE_URL}
      CONTROL_PLANE_AUTH_COOKIE_SECURE: ${CONTROL_PLANE_AUTH_COOKIE_SECURE:-false}
      GOOGLE_OAUTH_CLIENT_ID: ${GOOGLE_OAUTH_CLIENT_ID:-}
      GOOGLE_OAUTH_CLIENT_SECRET: ${GOOGLE_OAUTH_CLIENT_SECRET:-}
      GOOGLE_OAUTH_REDIRECT_URI: ${GOOGLE_OAUTH_REDIRECT_URI:-}
      CONTROL_PLANE_PROVIDER_CREDENTIAL_ENV_MAP: ${CONTROL_PLANE_PROVIDER_CREDENTIAL_ENV_MAP:-}
      GATELM_DEMO_PROVIDER_MODE: ${GATELM_DEMO_PROVIDER_MODE:-mock}
      GATELM_DEMO_API_KEY: ${GATELM_DEMO_API_KEY:-}
      GATELM_DEMO_APP_TOKEN: ${GATELM_DEMO_APP_TOKEN:-}
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public
      REDIS_URL: redis://redis:6379
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    ports:
      - "${AWS_TRIAGE_CONTROL_PLANE_PORT}:3001"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:3001/healthz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    networks:
      - gatelm_internal

  gateway-core:
    image: gatelm/gateway-core:aws-triage
    build:
      context: ../..
      dockerfile: infra/docker/gateway-core.Dockerfile
    restart: unless-stopped
    environment:
      GATEWAY_PORT: "8080"
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public
      REDIS_URL: redis://redis:6379
      GATEWAY_CONTROL_PLANE_BASE_URL: http://control-plane-api:3001
      GATEWAY_CONTROL_PLANE_TIMEOUT_MS: ${GATEWAY_CONTROL_PLANE_TIMEOUT_MS}
      GATEWAY_RUNTIME_SNAPSHOT_MODE: ${GATEWAY_RUNTIME_SNAPSHOT_MODE}
      MOCK_PROVIDER_BASE_URL: http://mock-provider:8090
      GATEWAY_DEFAULT_PROVIDER: ${GATEWAY_DEFAULT_PROVIDER}
      GATEWAY_DEFAULT_MODEL: ${GATEWAY_DEFAULT_MODEL}
      GATEWAY_LOW_COST_MODEL: ${GATEWAY_LOW_COST_MODEL}
      GATEWAY_HIGH_QUALITY_MODEL: ${GATEWAY_HIGH_QUALITY_MODEL}
      GATEWAY_EXACT_CACHE_KEY_SECRET: ${GATEWAY_EXACT_CACHE_KEY_SECRET}
      GATEWAY_MAX_REQUEST_BODY_BYTES: ${GATEWAY_MAX_REQUEST_BODY_BYTES}
      GATEWAY_RATE_LIMIT_ENABLED: ${GATEWAY_RATE_LIMIT_ENABLED}
      GATEWAY_RATE_LIMIT_BACKEND: ${GATEWAY_RATE_LIMIT_BACKEND}
      GATEWAY_RATE_LIMIT_ALGORITHM: ${GATEWAY_RATE_LIMIT_ALGORITHM}
      GATEWAY_RATE_LIMIT_WINDOW_SECONDS: ${GATEWAY_RATE_LIMIT_WINDOW_SECONDS}
      GATEWAY_RATE_LIMIT_LIMIT: ${GATEWAY_RATE_LIMIT_LIMIT}
      GATEWAY_RATE_LIMIT_REDIS_KEY_PREFIX: ${GATEWAY_RATE_LIMIT_REDIS_KEY_PREFIX}
      GATEWAY_AI_SAFETY_SIDECAR_ENABLED: ${GATEWAY_AI_SAFETY_SIDECAR_ENABLED}
      GATEWAY_AI_SAFETY_SIDECAR_URL: http://ai-service:8001/internal/ai-safety/v1/detect
      GATEWAY_PROVIDER_CREDENTIAL_ENV_MAP: ${GATEWAY_PROVIDER_CREDENTIAL_ENV_MAP:-}
      GATELM_DEMO_API_KEY: ${GATELM_DEMO_API_KEY}
      GATELM_DEMO_APP_TOKEN: ${GATELM_DEMO_APP_TOKEN}
      GATELM_DEMO_TENANT_ID: ${GATELM_DEMO_TENANT_ID}
      GATELM_DEMO_PROJECT_ID: ${GATELM_DEMO_PROJECT_ID}
      GATELM_DEMO_APPLICATION_ID: ${GATELM_DEMO_APPLICATION_ID}
      GATELM_DEMO_API_KEY_ID: ${GATELM_DEMO_API_KEY_ID}
      GATELM_DEMO_APP_TOKEN_ID: ${GATELM_DEMO_APP_TOKEN_ID}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    ports:
      - "${AWS_TRIAGE_GATEWAY_PORT}:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mock-provider:
        condition: service_healthy
      control-plane-api:
        condition: service_healthy
      ai-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "grep -aq gateway-core /proc/1/cmdline"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - gatelm_internal

  ai-service:
    image: gatelm/ai-service:aws-triage
    build:
      context: ../..
      dockerfile: infra/docker/ai-service.Dockerfile
      args:
        AI_SERVICE_INSTALL_ML_DEPS: ${AI_SERVICE_INSTALL_ML_DEPS}
    restart: unless-stopped
    environment:
      AI_SERVICE_HOST: 0.0.0.0
      AI_SERVICE_PORT: "8001"
      AI_SERVICE_LOG_LEVEL: ${AI_SERVICE_LOG_LEVEL}
      AI_SERVICE_REMOTE_SAFETY_MODE: ${AI_SERVICE_REMOTE_SAFETY_MODE}
      AI_SERVICE_ACCESS_LOG_ENABLED: ${AI_SERVICE_ACCESS_LOG_ENABLED}
      TRANSFORMERS_OFFLINE: ${AI_SERVICE_TRANSFORMERS_OFFLINE}
      HF_HUB_OFFLINE: ${AI_SERVICE_TRANSFORMERS_OFFLINE}
      AI_SERVICE_AI_SAFETY_DETECTOR_RUNTIME: ${AI_SERVICE_AI_SAFETY_DETECTOR_RUNTIME}
      AI_SERVICE_AI_SAFETY_DETECTOR_MODEL_ID: ${AI_SERVICE_AI_SAFETY_DETECTOR_MODEL_ID}
      AI_SERVICE_AI_SAFETY_ADDITIONAL_DETECTOR_MODEL_IDS: ${AI_SERVICE_AI_SAFETY_ADDITIONAL_DETECTOR_MODEL_IDS}
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8001/healthz', timeout=2).read()\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - gatelm_internal

  application:
    image: gatelm/application:aws-triage
    build:
      context: ../..
      dockerfile: infra/docker/application.Dockerfile
    restart: unless-stopped
    environment:
      PORT: "3002"
      HOSTNAME: 0.0.0.0
      GATELM_WEB_CUSTOMER_DEMO_MODE: ${GATELM_WEB_CUSTOMER_DEMO_MODE}
      GATELM_GATEWAY_BASE_URL: http://gateway-core:8080
      GATELM_GATEWAY_API_KEY: ${GATELM_DEMO_API_KEY}
      GATELM_DEMO_API_KEY: ${GATELM_DEMO_API_KEY}
      GATELM_DEMO_PROJECT_ID: ${GATELM_DEMO_PROJECT_ID}
      GATELM_APPLICATION_CHAT_MAX_TOKENS: ${GATELM_APPLICATION_CHAT_MAX_TOKENS}
      GATELM_WEB_RATE_LIMIT_MAX_ATTEMPTS: ${GATELM_WEB_RATE_LIMIT_MAX_ATTEMPTS}
      GATELM_CONTROL_PLANE_BASE_URL: http://control-plane-api:3001
      GATELM_CONTROL_PLANE_TENANT_ID: ${GATELM_DEMO_TENANT_ID}
      GATELM_CONTROL_PLANE_PROJECT_ID: ${GATELM_DEMO_PROJECT_ID}
      GATELM_CONTROL_PLANE_APPLICATION_ID: ${GATELM_DEMO_APPLICATION_ID}
      GATELM_PROVIDER_FAILURE_CONTROL_URL: http://mock-provider:8090
    ports:
      - "${AWS_TRIAGE_APPLICATION_PORT}:3002"
    depends_on:
      control-plane-api:
        condition: service_healthy
      gateway-core:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:3002/').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - gatelm_internal

  web:
    image: gatelm/web:aws-triage
    build:
      context: ../..
      dockerfile: infra/docker/web.Dockerfile
    restart: unless-stopped
    environment:
      PORT: "3000"
      HOSTNAME: 0.0.0.0
      GATELM_PUBLIC_DOMAIN: ${GATELM_PUBLIC_DOMAIN}
      GATELM_PUBLIC_BASE_URL: ${GATELM_PUBLIC_BASE_URL}
      GATELM_APPLICATION_BASE_URL: ${GATELM_APPLICATION_BASE_URL}
      GATELM_CONTROL_PLANE_BASE_URL: http://control-plane-api:3001
      GATELM_GATEWAY_BASE_URL: http://gateway-core:8080
      GATELM_CONTROL_PLANE_TENANT_ID: ${GATELM_DEMO_TENANT_ID}
      GATELM_CONTROL_PLANE_PROJECT_ID: ${GATELM_DEMO_PROJECT_ID}
      GATELM_CONTROL_PLANE_APPLICATION_ID: ${GATELM_DEMO_APPLICATION_ID}
      GATELM_DEMO_API_KEY: ${GATELM_DEMO_API_KEY}
      GATELM_DEMO_PROJECT_ID: ${GATELM_DEMO_PROJECT_ID}
      GATELM_WEB_CUSTOMER_DEMO_MODE: ${GATELM_WEB_CUSTOMER_DEMO_MODE}
      GATELM_WEB_RATE_LIMIT_MAX_ATTEMPTS: ${GATELM_WEB_RATE_LIMIT_MAX_ATTEMPTS}
    ports:
      - "${AWS_TRIAGE_WEB_PORT}:3000"
    depends_on:
      control-plane-api:
        condition: service_healthy
      gateway-core:
        condition: service_healthy
      application:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:3000/').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - gatelm_internal

volumes:
  postgres_data:
  redis_data:

networks:
  gatelm_internal:
    driver: bridge
