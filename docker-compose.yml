name: gatelm

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: gatelm
      POSTGRES_PASSWORD: gatelm
      POSTGRES_DB: gatelm
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gatelm -d gatelm"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  mock-provider:
    image: python:3.12-alpine
    environment:
      MOCK_PROVIDER_PORT: ${MOCK_PROVIDER_PORT:-8090}
      MOCK_PROVIDER_DEFAULT_LATENCY_MS: ${MOCK_PROVIDER_DEFAULT_LATENCY_MS:-150}
      MOCK_PROVIDER_ERROR_MODE: ${MOCK_PROVIDER_ERROR_MODE:-off}
      MOCK_PROVIDER_STATS_HASH_SECRET: ${MOCK_PROVIDER_STATS_HASH_SECRET:-mock_provider_stats_hash_secret_for_p0_demo_only}
    command:
      - python
      - -c
      - |
        import copy, hashlib, hmac, json, os, threading, time
        from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer

        port = int(os.getenv("MOCK_PROVIDER_PORT", "8090"))
        latency_ms = int(os.getenv("MOCK_PROVIDER_DEFAULT_LATENCY_MS", "150"))
        initial_error_mode = os.getenv("MOCK_PROVIDER_ERROR_MODE", "off").strip().lower()
        if initial_error_mode not in ["off", "error", "timeout"]:
            initial_error_mode = "off"
        stats_hash_secret = os.getenv("MOCK_PROVIDER_STATS_HASH_SECRET", "mock_provider_stats_hash_secret_for_p0_demo_only").encode("utf-8")
        state_lock = threading.Lock()
        state = {
            "calls": 0,
            "config": {"mode": initial_error_mode, "failModels": []},
            "data": {"totalCalls": 0, "callsByModel": {}, "lastCalls": []}
        }

        class ConcurrentHTTPServer(ThreadingHTTPServer):
            daemon_threads = True
            request_queue_size = 128

        def reset_state():
            with state_lock:
                state["calls"] = 0
                state["data"]["totalCalls"] = 0
                state["data"]["callsByModel"] = {}
                state["data"]["lastCalls"] = []

        def configure_failure(payload):
            if not isinstance(payload, dict):
                return False
            mode = str(payload.get("mode", "off")).strip().lower()
            if mode not in ["off", "error", "timeout"]:
                return False
            fail_models = payload.get("failModels") or []
            if not isinstance(fail_models, list):
                return False
            with state_lock:
                state["config"] = {
                    "mode": mode,
                    "failModels": [str(model).strip() for model in fail_models if str(model).strip()]
                }
            return True

        def failure_mode_for_model(model):
            with state_lock:
                config = copy.deepcopy(state.get("config", {}))
            mode = config.get("mode", "off")
            if mode == "off":
                return "off"
            fail_models = config.get("failModels") or []
            model_name = str(model).strip()
            return mode if not fail_models or model_name in fail_models else "off"

        def prompt_hash(prompt):
            digest = hmac.new(stats_hash_secret, prompt.encode("utf-8"), hashlib.sha256).hexdigest()
            return "hmac-sha256:" + digest

        def record_call(request_id, model, prompt):
            call = {
                "requestId": request_id,
                "model": model,
                "promptHash": prompt_hash(prompt),
                "createdAt": time.strftime("%Y-%m-%dT%H:%M:%S.000Z", time.gmtime()),
            }
            with state_lock:
                state["calls"] += 1
                data = state["data"]
                data["totalCalls"] = state["calls"]
                data["callsByModel"][model] = data["callsByModel"].get(model, 0) + 1
                data["lastCalls"].append(call)
                data["lastCalls"] = data["lastCalls"][-20:]

        def state_snapshot():
            with state_lock:
                return copy.deepcopy(state)

        def stream_chunks(model):
            created = int(time.time())
            return [
                {
                    "id": "mock_chatcmpl_stream_local",
                    "object": "chat.completion.chunk",
                    "created": created,
                    "model": model,
                    "choices": [{"index": 0, "delta": {"role": "assistant"}, "finish_reason": None}],
                },
                {
                    "id": "mock_chatcmpl_stream_local",
                    "object": "chat.completion.chunk",
                    "created": created,
                    "model": model,
                    "choices": [{"index": 0, "delta": {"content": "GateLM mock streaming 응답입니다. "}, "finish_reason": None}],
                },
                {
                    "id": "mock_chatcmpl_stream_local",
                    "object": "chat.completion.chunk",
                    "created": created,
                    "model": model,
                    "choices": [{"index": 0, "delta": {"content": "실제 Provider 비용 없이 SSE relay를 확인합니다."}, "finish_reason": None}],
                },
                {
                    "id": "mock_chatcmpl_stream_local",
                    "object": "chat.completion.chunk",
                    "created": created,
                    "model": model,
                    "choices": [{"index": 0, "delta": {}, "finish_reason": "stop"}],
                    "usage": {"prompt_tokens": 32, "completion_tokens": 24, "total_tokens": 56},
                },
            ]

        class Handler(BaseHTTPRequestHandler):
            def send_json(self, status, payload):
                body = json.dumps(payload).encode("utf-8")
                self.send_response(status)
                self.send_header("Content-Type", "application/json")
                self.send_header("Content-Length", str(len(body)))
                self.end_headers()
                self.wfile.write(body)

            def send_stream(self, model):
                self.send_response(200)
                self.send_header("Content-Type", "text/event-stream; charset=utf-8")
                self.send_header("Cache-Control", "no-cache")
                self.send_header("Connection", "keep-alive")
                self.send_header("X-Accel-Buffering", "no")
                self.end_headers()
                for chunk in stream_chunks(model):
                    data = json.dumps(chunk, ensure_ascii=False)
                    self.wfile.write(("data: " + data + "\n\n").encode("utf-8"))
                    self.wfile.flush()
                    time.sleep(0.02)
                self.wfile.write(b"data: [DONE]\n\n")
                self.wfile.flush()

            def do_GET(self):
                if self.path == "/healthz":
                    self.send_json(200, {"status": "ok"})
                    return
                if self.path == "/v1/models":
                    self.send_json(200, {"object": "list", "data": [
                        {"id": "mock-fast", "object": "model", "owned_by": "mock"},
                        {"id": "mock-balanced", "object": "model", "owned_by": "mock"},
                        {"id": "mock-smart", "object": "model", "owned_by": "mock"}
                    ]})
                    return
                if self.path == "/__mock/stats":
                    self.send_json(200, state_snapshot())
                    return
                self.send_json(404, {"error": {"code": "not_found"}})

            def do_POST(self):
                length = int(self.headers.get("Content-Length", "0"))
                raw = self.rfile.read(length) if length else b"{}"
                try:
                    payload = json.loads(raw.decode("utf-8"))
                except Exception:
                    payload = {}
                if self.path == "/__mock/reset":
                    reset_state()
                    self.send_json(200, {"ok": True})
                    return
                if self.path == "/__mock/config":
                    if not configure_failure(payload):
                        self.send_json(400, {"error": {"code": "invalid_mock_config"}})
                        return
                    self.send_json(200, {"ok": True})
                    return
                if self.path == "/v1/chat/completions":
                    if not isinstance(payload, dict):
                        self.send_json(400, {"error": {"code": "invalid_request"}})
                        return
                    time.sleep(latency_ms / 1000)
                    model = str(payload.get("model") or "mock-balanced").strip() or "mock-balanced"
                    messages = payload.get("messages") or []
                    prompt = " ".join(str(m.get("content", "")) for m in messages if isinstance(m, dict))
                    failure_mode = failure_mode_for_model(model)
                    if failure_mode != "off":
                        status = 504 if failure_mode == "timeout" else 500
                        code = "provider_timeout" if failure_mode == "timeout" else "provider_error"
                        self.send_json(status, {"error": {"code": code, "message": "Synthetic provider failure for GateLM evidence."}})
                        return
                    record_call(self.headers.get("X-GateLM-Request-Id", ""), model, prompt)
                    if payload.get("stream") is True:
                        self.send_stream(model)
                        return
                    self.send_json(200, {
                        "id": "mock_chatcmpl_local",
                        "object": "chat.completion",
                        "created": int(time.time()),
                        "model": model,
                        "choices": [{"index": 0, "message": {"role": "assistant", "content": "Mock response generated by GateLM local mock provider."}, "finish_reason": "stop"}],
                        "usage": {"prompt_tokens": 32, "completion_tokens": 24, "total_tokens": 56}
                    })
                    return
                self.send_json(404, {"error": {"code": "not_found"}})

        ConcurrentHTTPServer(("0.0.0.0", port), Handler).serve_forever()
    ports:
      - "${MOCK_PROVIDER_PORT:-8090}:8090"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8090/healthz', timeout=2).read()\""]
      interval: 5s
      timeout: 3s
      retries: 10

  prometheus:
    image: prom/prometheus:v2.55.1

    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=${PROMETHEUS_RETENTION_TIME:-24h}
      - --web.console.libraries=/usr/share/prometheus/console_libraries
      - --web.console.templates=/usr/share/prometheus/consoles
      - --web.enable-lifecycle
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./infra/observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./infra/observability/prometheus/rules:/etc/prometheus/rules:ro
      - prometheus_data:/prometheus
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:9090/-/healthy || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10

  grafana:
    image: grafana/grafana:11.3.0
    depends_on:
      prometheus:
        condition: service_started
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_ANALYTICS_REPORTING_ENABLED: "false"
      GF_ANALYTICS_CHECK_FOR_UPDATES: "false"
    ports:
      - "${GRAFANA_PORT:-3005}:3000"
    volumes:
      - ./infra/observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./infra/observability/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana_data:/var/lib/grafana
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:3000/api/health || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10
  node-toolbox:
    profiles: ["tools"]
    build:
      context: .
      dockerfile: infra/docker/node/Dockerfile
      args:
        NODE_VERSION: ${NODE_VERSION:-22}
        PNPM_VERSION: ${PNPM_VERSION:-9.15.0}
    environment:
      CONTROL_PLANE_PORT: ${CONTROL_PLANE_PORT:-3001}
      DATABASE_URL: postgresql://gatelm:gatelm@postgres:5432/gatelm?schema=public
      REDIS_URL: redis://redis:6379
      CONTROL_PLANE_ADMIN_AUTH_MODE: ${CONTROL_PLANE_ADMIN_AUTH_MODE:-session_cookie}
      CONTROL_PLANE_INTERNAL_SERVICE_TOKEN: ${CONTROL_PLANE_INTERNAL_SERVICE_TOKEN:-local-control-plane-internal-token-for-dev-only}
      CONTROL_PLANE_AUTH_STATE_SECRET: ${CONTROL_PLANE_AUTH_STATE_SECRET:?CONTROL_PLANE_AUTH_STATE_SECRET is required}
      CONTROL_PLANE_PROVIDER_CREDENTIAL_ENV_MAP: ${CONTROL_PLANE_PROVIDER_CREDENTIAL_ENV_MAP:-}
      GATEWAY_PROVIDER_CREDENTIAL_ENV_MAP: ${GATEWAY_PROVIDER_CREDENTIAL_ENV_MAP:-}
      GATELM_PROVIDER_CREDENTIAL_ENCRYPTION_KEY: ${GATELM_PROVIDER_CREDENTIAL_ENCRYPTION_KEY:-}
      GATELM_PROVIDER_CREDENTIAL_ENCRYPTION_KEY_VERSION: ${GATELM_PROVIDER_CREDENTIAL_ENCRYPTION_KEY_VERSION:-v1}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
    ports:
      - "${CONTROL_PLANE_PORT:-3001}:${CONTROL_PLANE_PORT:-3001}"
    working_dir: /workspace
    volumes:
      - .:/workspace
      - node_pnpm_store:/pnpm/store
      - application_next_cache:/workspace/apps/application/.next
    command: ["bash"]

  go-toolbox:
    profiles: ["tools"]
    build:
      context: .
      dockerfile: infra/docker/go/Dockerfile
      args:
        GO_VERSION: ${GO_VERSION:-1.24}
    environment:
      GATEWAY_PORT: ${GATEWAY_PORT:-8080}
      GATEWAY_DEFAULT_PROVIDER: ${GATEWAY_DEFAULT_PROVIDER:-mock}
      GATEWAY_DEFAULT_MODEL: ${GATEWAY_DEFAULT_MODEL:-mock-balanced}
      GATEWAY_LOW_COST_MODEL: ${GATEWAY_LOW_COST_MODEL:-mock-fast}
      GATEWAY_HIGH_QUALITY_MODEL: ${GATEWAY_HIGH_QUALITY_MODEL:-mock-smart}
      GATEWAY_PROVIDER_TIMEOUT_MS: ${GATEWAY_PROVIDER_TIMEOUT_MS:-5000}
      GATEWAY_MAX_REQUEST_BODY_BYTES: ${GATEWAY_MAX_REQUEST_BODY_BYTES:-4194304}
      GATEWAY_CONTROL_PLANE_BASE_URL: ${GATEWAY_CONTROL_PLANE_BASE_URL:-http://host.docker.internal:${CONTROL_PLANE_PORT:-3001}}
      GATEWAY_CONTROL_PLANE_INTERNAL_TOKEN: ${GATEWAY_CONTROL_PLANE_INTERNAL_TOKEN:-local-control-plane-internal-token-for-dev-only}
      GATEWAY_CONTROL_PLANE_TIMEOUT_MS: ${GATEWAY_CONTROL_PLANE_TIMEOUT_MS:-2000}
      GATEWAY_PROVIDER_CREDENTIAL_ENV_MAP: ${GATEWAY_PROVIDER_CREDENTIAL_ENV_MAP:-}
      GATELM_PROVIDER_CREDENTIAL_ENCRYPTION_KEY: ${GATELM_PROVIDER_CREDENTIAL_ENCRYPTION_KEY:-}
      GATELM_PROVIDER_CREDENTIAL_ENCRYPTION_KEY_VERSION: ${GATELM_PROVIDER_CREDENTIAL_ENCRYPTION_KEY_VERSION:-v1}
      GATEWAY_AI_SAFETY_SIDECAR_ENABLED: ${GATEWAY_AI_SAFETY_SIDECAR_ENABLED:-false}
      GATEWAY_AI_SAFETY_SIDECAR_URL: ${GATEWAY_AI_SAFETY_SIDECAR_URL:-http://host.docker.internal:8001/internal/ai-safety/v1/detect}
      GATEWAY_AI_SAFETY_SIDECAR_TIMEOUT_MS: ${GATEWAY_AI_SAFETY_SIDECAR_TIMEOUT_MS:-300}
      GATEWAY_AI_SAFETY_SIDECAR_MODEL_ID: ${GATEWAY_AI_SAFETY_SIDECAR_MODEL_ID:-openai/privacy-filter}
      GATEWAY_AI_SAFETY_SIDECAR_DETECTOR_SET: ${GATEWAY_AI_SAFETY_SIDECAR_DETECTOR_SET:-privacy-filter-default}
      GATEWAY_AI_SAFETY_SIDECAR_LOCALE: ${GATEWAY_AI_SAFETY_SIDECAR_LOCALE:-ko-KR}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      DATABASE_URL: postgresql://gatelm:gatelm@postgres:5432/gatelm?schema=public
      REDIS_URL: redis://redis:6379
      MOCK_PROVIDER_BASE_URL: http://mock-provider:8090
    ports:
      - "${GATEWAY_PORT:-8080}:${GATEWAY_PORT:-8080}"
    working_dir: /workspace
    volumes:
      - .:/workspace
      - go_pkg_cache:/go/pkg/mod
      - go_build_cache:/root/.cache/go-build
    command: ["bash"]

  python-toolbox:
    profiles: ["tools"]
    build:
      context: .
      dockerfile: infra/docker/python/Dockerfile
      args:
        PYTHON_VERSION: ${PYTHON_VERSION:-3.12}
    working_dir: /workspace
    environment:
      AI_SERVICE_HOST: ${AI_SERVICE_HOST:-0.0.0.0}
      AI_SERVICE_PORT: ${AI_SERVICE_PORT:-8001}
      AI_SERVICE_ACCESS_LOG_ENABLED: ${AI_SERVICE_ACCESS_LOG_ENABLED:-false}
      AI_SERVICE_TRANSFORMERS_OFFLINE: ${AI_SERVICE_TRANSFORMERS_OFFLINE:-1}
      TRANSFORMERS_OFFLINE: ${TRANSFORMERS_OFFLINE:-1}
      HF_HUB_OFFLINE: ${HF_HUB_OFFLINE:-1}
      AI_SERVICE_AI_SAFETY_DETECTOR_RUNTIME: ${AI_SERVICE_AI_SAFETY_DETECTOR_RUNTIME:-onnx}
      AI_SERVICE_AI_SAFETY_DETECTOR_MODEL_ID: ${AI_SERVICE_AI_SAFETY_DETECTOR_MODEL_ID:-.cache/onnx/openai--privacy-filter}
      AI_SERVICE_AI_SAFETY_ADDITIONAL_DETECTOR_MODEL_IDS: ${AI_SERVICE_AI_SAFETY_ADDITIONAL_DETECTOR_MODEL_IDS:-.cache/onnx/amoeba04--koelectra-small-v3-privacy-ner-quantized}
    ports:
      - "${AI_SERVICE_PORT:-8001}:${AI_SERVICE_PORT:-8001}"
    volumes:
      - .:/workspace
      - python_pip_cache:/root/.cache/pip
    command: ["bash"]

volumes:
  postgres_data:
  redis_data:
  prometheus_data:
  grafana_data:
  node_pnpm_store:
  application_next_cache:
  go_pkg_cache:
  go_build_cache:
  python_pip_cache:
