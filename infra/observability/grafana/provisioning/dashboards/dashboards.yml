apiVersion: 1

providers:
  - name: GateLM
    orgId: 1
    folder: GateLM
    folderUid: gatelm
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards